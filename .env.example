# Set this as a Vercel Environment Variable for Preview + Production.
# Do not commit the real value.
TEACHER_PUBLISH_KEY=replace-with-a-long-random-secret

# Vercel Blob authentication is supplied automatically when a Blob store
# is connected to the project (OIDC on current Vercel Blob setups).
